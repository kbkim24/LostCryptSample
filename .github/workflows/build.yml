name: SonarQube Analysis

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  sonar:
    runs-on: windows-latest  # Windows 환경 필요
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '6.0.x'  # 필요하면 7.0.x로 변경

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v1

      - name: Restore NuGet packages
        run: nuget restore YourSolution.sln

      - name: SonarScanner Begin
        run: |
          SonarScanner.MSBuild.exe begin /k:"YourProjectKey" /d:sonar.host.url="https://sonarcloud.io" /d:sonar.token="${{ secrets.SONAR_TOKEN }}"
        shell: cmd

      - name: Build Solution
        run: msbuild YourSolution.sln /p:Configuration=Release /t:Rebuild
        shell: cmd

      - name: SonarScanner End
        run: |
          SonarScanner.MSBuild.exe end /d:sonar.token="${{ secrets.SONAR_TOKEN }}"
        shell: cmd

